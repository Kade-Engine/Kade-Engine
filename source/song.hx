package;

class OptionsSubState extends MusicBeatSubstate
{
	var textMenuItems:Array<String> = ['Master Volume', 'Sound Volume'];

	public function new(file)
	{
		//music list
		new(SONG);
		add(test);
	}
}

class songAdjust:Volume
{
	// Master Volume = 100;
	// Sound Volume += 20;
	// test + newVolume
	// test + newMasterVolume

	// test(Master Voume = 100;)
}